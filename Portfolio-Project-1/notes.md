Created labadmin as sudo
Disabled sudo
Disabled password auth
Enabled key-pair auth
Used ufw to do default deny and enable only ssh port
used fail2ban to check, intrusion prevention software, to ban kali 

enabled password auth for this
Used nmap to scan for open ports
used iptables to configure firewall with more depth
enabled loopback traffic
used nmap scan to check after
